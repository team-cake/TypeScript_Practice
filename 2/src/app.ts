const button = document.querySelector('button')!

button.addEventListener('click', () => {
	console.log('Clicked!')
})

const map = new Map()

let age: number
age = 20

const userName = 'Alex'

console.log(userName)
